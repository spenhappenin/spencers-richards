import React from 'react';
import { Container, Header, } from '../../styles/shared';

class Modcom extends React.Component {
  render() {
    return (
      <Container>
        <Header>Modcom</Header>
      </Container>
    );
  };
};

export default Modcom;
