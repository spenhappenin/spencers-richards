import React from 'react';
import { Container, Header, } from '../../styles/shared';

class BountyBuilder extends React.Component {
  render() {
    return (
      <Container>
        <Header>Bounty Builder</Header>
      </Container>
    );
  };
};

export default BountyBuilder;
