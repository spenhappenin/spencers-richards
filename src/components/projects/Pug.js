import React from 'react';
import { Container, Header, } from '../../styles/shared';

class Pug extends React.Component {
  render() {
    return (
      <Container>
        <Header>Pick Up Games</Header>
      </Container>
    );
  };
};

export default Pug;
